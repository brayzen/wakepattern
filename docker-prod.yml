version: '3'
services:
  db:
    image: postgres
    volumes:
      - wakepatternprod.cvyu4ku1saus.us-west-2.rds.amazonaws.com
  web:
    build: .
    command: bundle exec rails s -p 80 -b '0.0.0.0'
    volumes:
      - .:/myapp
    ports:
      - "80:80"
    depends_on:
      - db
